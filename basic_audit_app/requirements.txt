# Basic audit app dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.24.0

# Install bh-fastapi-audit from local checkout (sibling directory):
#   pip install -e ../bh-fastapi-audit
#
# Or from PyPI (once published):
#   pip install bh-fastapi-audit>=0.2.2
