# DynamoDB audit app dependencies
fastapi>=0.100.0,<1
uvicorn[standard]>=0.24.0,<1
boto3>=1.34,<2

# Install bh-fastapi-audit with DynamoDB support from local checkout:
#   pip install -e "../bh-fastapi-audit[dynamodb]"
#
# Or from PyPI (once published):
#   pip install "bh-fastapi-audit[dynamodb]>=0.5.0"
